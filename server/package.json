{
  "name": "eenytdwn-server",
  "version": "1.0.0",
  "description": "Server for YouTube video downloader",
  "scripts": {
    "build": "cd functions && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
